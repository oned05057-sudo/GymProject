/**
 * prisma.config.ts
 * Prisma 7: move connection URLs for Migrate into this file.
 *
 * Usage:
 *  - For Migrate: the `migrate.url` value will be used by `prisma migrate`.
 *  - For PrismaClient: pass connection via `adapter.url` or use `accelerateUrl` for Prisma Accelerate.
 *
 * Set your DATABASE_URL in environment variables for production. For local development with SQLite
 * you can keep the fallback 'file:./dev.db' below.
 */

const config = {};

export default config;
